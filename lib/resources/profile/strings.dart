// Headings

abstract class HEADINGS {
  static const heading = 'Workplaces';
}

// Subheadings

abstract class SUB_HEADINGS {
  static const payment_details = 'Payment Details';
}

// Paragraphs

abstract class PARAGRAPHS {
  static const to = 'to';
}

// Labels

abstract class LABELS {}

// Errors

abstract class ERRORS {}

// Links

abstract class LINKS {
  static const logout = 'Logout';
}
