// Headings

abstract class HEADINGS {
  static const screen_name = 'My Dashboard';
  static const total_billing = 'TOTAL BILLING';
  static const pending_payments = 'PENDING PAYMENTS';
}

// Subheadings

abstract class SUB_HEADINGS {
  static const billing_this_month = 'Billing this Month:';
  static const lifetime_billing = 'Lifetime Billing:';

  static const payments_pending = 'No. of payments pending';
  static const value_pending_payments = 'Value of payments pending';
}

// Paragraphs

abstract class PARAGRAPHS {}

// Labels

abstract class LABELS {}

// Links

abstract class LINKS {}
