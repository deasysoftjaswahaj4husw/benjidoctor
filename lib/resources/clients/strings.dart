// Headings

abstract class HEADINGS {
  static const my_clients = 'My Clients';
  static const no_clients = 'You have no clients on the Benji Platform yet.';
  static const add_client = 'Add a new client';
}

// Subheadings

abstract class SUB_HEADINGS {
  static const add_client =
      'Enter Details of your new client so you can start scheduling your sessions with them. ';
}

// Paragraphs

abstract class PARAGRAPHS {}

// Labels

abstract class LABELS {
  static const add_client = 'Add My Clients';

  static const name = 'Name';
  static const phone = 'Phone Number';
  static const email = 'Email ID';
  static const diagnosis = 'Concern/Diagnosis';
}

// Links

abstract class LINKS {}
