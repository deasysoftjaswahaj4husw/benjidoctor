// Headings

abstract class HEADINGS {
  static const heading = 'Enter OTP Sent to';
  static const phone_number = 'XXXXXXXXXX';
}

// Subheadings

abstract class SUB_HEADINGS {}

// Paragraphs

abstract class PARAGRAPHS {}

// Labels

abstract class LABELS {}

// Errors

abstract class ERRORS {
  static const incorrect_otp = 'OTP entered was incorrect';
}

// Links

abstract class LINKS {
  static const resend_otp = 'Resend OTP';
  static const next = 'Next';
}
