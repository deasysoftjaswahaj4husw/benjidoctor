// Headings

abstract class HEADINGS {
  static const heading = 'Enter your phone number';
}

// Subheadings

abstract class SUB_HEADINGS {}

// Paragraphs

abstract class PARAGRAPHS {}

// Labels

abstract class LABELS {
  static const phone_number = 'Phone Number';
}

// Errors

abstract class ERRORS {
  static const account_exists = 'Your account already exists';
  static const invalid_phone_number = 'Invalid Phone Number';
}

// Links

abstract class LINKS {
  static const next = 'Next';
}
